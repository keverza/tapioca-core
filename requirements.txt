# Tapioca Python runtime dependencies.
#
# The native add-on ships the `evp`/`tapioca` package and embeds CPython. The
# managed runtime is installed with `dist/Install-Runtime.ps1`; these entries
# document the same baseline for an external or development Python environment.
# The public examples themselves use only the shipped Tapioca API and stdlib.
#
# Do not install these into a system Python expecting Archicad to use them. The
# add-on uses its managed runtime under %LOCALAPPDATA%\Tapioca\runtime.

# --- Minimum managed runtime ------------------------------------------------- #
# Keep this list synchronized with evp._env.BASELINE and
# dist/Install-Runtime.ps1 -Baseline. numpy backs evp.geometry; pydantic backs
# schema-style command ports; pillow and requests are part of the shipped
# runtime baseline used by the current command/API surface.
numpy==2.0.2
pillow
requests
pydantic>=2.7,<3

# --- Optional command dependencies ------------------------------------------ #
# Commands declare these with @tapioca.command(requires=[...]); the managed
# runtime installs them on first use. They are not needed for the add-on or
# the public examples to load.
ezdxf                   # ClearanceHeatmap, MeshFromContour, TreesFromDxf
reportlab==4.2.5        # MassingFeasibility PDF output

# --- Offline development and tests ------------------------------------------ #
pytest                  # AddOn/EvP/tests and command helper tests
