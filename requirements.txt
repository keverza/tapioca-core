# Archicad Python scripts -- ALL dependencies (every folder).
#
#   pip install -r requirements.txt
#
# Per-folder subsets are in each folder's own requirements.txt, so you can install
# only what a given tier needs.


archicad                # official Graphisoft Python API (the built-in palette)
multiconn-archicad      # UnusedViewCleaner: multi-instance connection helper


pillow                  # MassingFeasibility: screenshot compositing (PIL)
# archicad              # (already above) AutomaticNumberingBasedOnPolyline


requests                # geomclient: HTTP to the GeometryServer add-on
msgpack                 # geomclient: mesh payloads
numpy                   # geometry maths / ray batches
ezdxf                   # ClearanceHeatmap: DXF output
# pillow                # (already above) MassingFeasibilityGS: image compositing

# --- dev / testing (not needed at runtime) ---------------------------------- #
pytest                  # offline unit tests: EvP command folders (e.g. MassingFeasibility/
                        # test_massingcalc.py), AddOn/EvP/tests/. Run: pytest -q
